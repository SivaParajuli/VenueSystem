server:
  port: 8888

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/vbsdb


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true